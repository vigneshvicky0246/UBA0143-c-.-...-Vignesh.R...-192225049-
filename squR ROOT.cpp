#include <iostream>
#include <cmath>

using namespace std;

int main() 
{
    float num, sqrt;
    cin >> num;
    if (num < 0) 
        cout << "Invalid input.";
     else 
        cout <<sqrt = pow(num, 0.5)<<"Square root is "<<sqrt;
    return 0;
}
